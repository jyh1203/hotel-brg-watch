{
  "name": "hotel-brg-watch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "collect": "node src/collect.mjs",
    "collect:full": "PLAYWRIGHT_HEADFUL=1 node src/collect.mjs",
    "build": "node src/build-dashboard.mjs",
    "dev": "node src/server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "playwright": "^1.55.0"
  }
}
